## What and why

<!-- The user-visible effect and the reason for the change. -->

## Checks

- [ ] `npm run format`, `npm run check` and `npm run test:import` pass locally
- [ ] No real medical images or patient details anywhere in the change
- [ ] The app still binds to `127.0.0.1` only and makes no network requests
- [ ] Docs updated if behaviour or controls changed
